clf;

L = [25e-3 56e-3 20e-3 65e-3 9.8e-3 50e-3 60e-3];

Ch = 15;
th3_angle = 10.75:0.2:19.55;

th3 = th3_angle*pi/180;

d1 = zeros(size(th3_angle));
th2 = d&;

for i = 0:size(th3_angle,2)

    [d1(i), th2(i)] = MGI(th3, L);
    [P(i), Rc, Ro, F] = contraintes(d1, th2, th3, L, Ch);
    Rcx(i) = Rc(1,1);
    Rcz(i) = Rc(3,1);

end





[th21, th22, th31, th32] = MGD(d1, L);

drawArm(d1, th21, th31, L, Ch);